/**
 * Local Expo Module entrypoint.
 *
 * App code should normally import from `@/native`; this barrel exists so the
 * local module also follows Expo's documented module layout.
 */
export * from '../../src/native';
export { flinkNative as default } from '../../src/native';
